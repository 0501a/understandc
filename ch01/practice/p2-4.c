#include <stdio.h>
int  main(void){
	int a=3,b;
	double x=3.1,y;
	printf("a*x=%f a*x=%d a/x=%d a/x=%.3f\n",a*x,a*x,a/x,a/x );
	scanf("%d,%lf",&b,&y);
	printf("b*y=%f b/y=%.3f\n",b*y,b/y );
	return 0;
}